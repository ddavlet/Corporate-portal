services:

  db:
    image: pgvector/pgvector:pg17-trixie
    restart: always
    container_name: db
    command: ["postgres", "-c", "timezone=Europe/Berlin"]
    environment:
      POSTGRES_DB: ${POSTGRES_DB}
      POSTGRES_USER: ${POSTGRES_USER}
      POSTGRES_PASSWORD: ${POSTGRES_PASSWORD}
      TZ: ${GENERIC_TIMEZONE}
    volumes:
      - pg_data:/var/lib/postgresql/data
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U $$POSTGRES_USER -d $$POSTGRES_DB"]
      interval: 10s
      timeout: 5s
      retries: 5
    # >>> CHANGE: db только во внутренней сети
    networks:
      - backend


  adminer:
    image: adminer:latest
    restart: always
    container_name: adminer
    depends_on:
      db:
        condition: service_started
    labels:
      - traefik.enable=true
      - traefik.docker.network=n8n_traefik-public
      - traefik.http.routers.adminer.rule=Host(`database.kolberg.uz`)
      - traefik.http.routers.adminer.tls=true
      - traefik.http.routers.adminer.entrypoints=web,websecure
      - traefik.http.routers.adminer.tls.certresolver=mytlschallenge
      - traefik.http.middlewares.adminer.headers.SSLRedirect=true
      - traefik.http.middlewares.adminer.headers.STSSeconds=315360000
      - traefik.http.middlewares.adminer.headers.browserXSSFilter=true
      - traefik.http.middlewares.adminer.headers.contentTypeNosniff=true
      - traefik.http.middlewares.adminer.headers.forceSTSHeader=true
      - traefik.http.middlewares.adminer.headers.SSLHost=kolberg.uz
      - traefik.http.middlewares.adminer.headers.STSIncludeSubdomains=true
      - traefik.http.middlewares.adminer.headers.STSPreload=true
      - traefik.http.routers.adminer.middlewares=adminer@docker
      - traefik.http.services.adminer.loadbalancer.server.port=8080
    # >>> CHANGE: adminer в public (для traefik) и backend (для db)
    networks:
      - traefik-public
      - backend


  traefik:
    image: "traefik"
    restart: always
    container_name: traefik
    command:
      - "--api.insecure=false"
      - "--providers.docker=true"
      - "--providers.docker.exposedbydefault=false"
      - "--entrypoints.web.address=:80"
      - "--entrypoints.web.http.redirections.entryPoint.to=websecure"
      - "--entrypoints.web.http.redirections.entrypoint.scheme=https"
      - "--entrypoints.websecure.address=:443"
      - "--certificatesresolvers.mytlschallenge.acme.tlschallenge=true"
      - "--certificatesresolvers.mytlschallenge.acme.email=${SSL_EMAIL}"
      - "--certificatesresolvers.mytlschallenge.acme.storage=/letsencrypt/acme.json"
      - "--log.level=DEBUG"

    ports:
      - "80:80"
      - "443:443"
    volumes:
      - traefik_data:/letsencrypt
      - /var/run/docker.sock:/var/run/docker.sock:ro

    labels:
      - traefik.enable=true
      - traefik.docker.network=whatsapp-api_traefik-public

      # Redirect only root path on old frontend domains: https://<sub>.kolberg.uz[/] -> https://<sub>.korberg.uz/app
      - traefik.http.routers.kolberg-frontend-root-redirect.rule=(${TRAEFIK_FRONTEND_HOST_RULE}) && Path(`/`)
      - traefik.http.routers.kolberg-frontend-root-redirect.entrypoints=web,websecure
      - traefik.http.routers.kolberg-frontend-root-redirect.tls=true
      - traefik.http.routers.kolberg-frontend-root-redirect.tls.certresolver=mytlschallenge
      - traefik.http.routers.kolberg-frontend-root-redirect.priority=1500
      - traefik.http.routers.kolberg-frontend-root-redirect.middlewares=kolberg-frontend-root-to-korberg@docker
      - traefik.http.middlewares.kolberg-frontend-root-to-korberg.redirectregex.regex=^https?://([a-z0-9-]+)\.kolberg\.uz/?$
      - traefik.http.middlewares.kolberg-frontend-root-to-korberg.redirectregex.replacement=https://$${1}.korberg.uz/app
      - traefik.http.middlewares.kolberg-frontend-root-to-korberg.redirectregex.permanent=true
    # >>> CHANGE: traefik только в public
    networks:
      - traefik-public

  browserless:
    image: browserless/chrome:latest
    restart: unless-stopped
    environment:
      - TOKEN=${BROWSERLESS_TOKEN}
      - MAX_CONCURRENT_SESSIONS=2
    networks:
      - chromium

  n8n:
    image: docker.n8n.io/n8nio/n8n
    restart: always
    container_name: n8n
    labels:
       - traefik.enable=true
       - traefik.docker.network=n8n_traefik-public
       # >>> CHANGE: можно убрать (не нужно для traefik)
       # - traefik.container.name=traefik
      # ---------- Shared security headers middleware ----------
       - traefik.http.middlewares.n8n.headers.SSLRedirect=true
       - traefik.http.middlewares.n8n.headers.STSSeconds=315360000
       - traefik.http.middlewares.n8n.headers.browserXSSFilter=true
       - traefik.http.middlewares.n8n.headers.contentTypeNosniff=true
       - traefik.http.middlewares.n8n.headers.forceSTSHeader=true
       - traefik.http.middlewares.n8n.headers.SSLHost=kolberg.uz
       - traefik.http.middlewares.n8n.headers.STSIncludeSubdomains=true
       - traefik.http.middlewares.n8n.headers.STSPreload=true

      # ---------- CORS middleware for dev.kolberg.uz  ----------
       - traefik.http.middlewares.n8n-dev-cors.headers.accessControlAllowOriginList=https://neuron.kolberg.uz,https://construction.kolberg.uz,https://lemonfit.kolberg.uz,https://lemonaqua.kolberg.uz,https://main.kolberg.uz,https://zarqand.kolberg.uz,https://invest.kolberg.uz,https://syrop.kolberg.uz,https://qulay.kolberg.uz,https://lemonelectro.kolberg.uz
       - traefik.http.middlewares.n8n-dev-cors.headers.accessControlAllowMethods=GET,POST,OPTIONS
       - traefik.http.middlewares.n8n-dev-cors.headers.accessControlAllowHeaders=Content-Type,Authorization
       - traefik.http.middlewares.n8n-dev-cors.headers.addVaryHeader=true

      # ---------- UI on dev.kolberg.uz ----------
       - traefik.http.routers.n8n-ui.rule=Host(`dev.kolberg.uz`)
       - traefik.http.routers.n8n-ui.entrypoints=web,websecure
       - traefik.http.routers.n8n-ui.tls=true
       - traefik.http.routers.n8n-ui.tls.certresolver=mytlschallenge
       - traefik.http.routers.n8n-ui.middlewares=n8n@docker,n8n-dev-cors@docker,n8n-error-pages@docker
       - traefik.http.routers.n8n-ui.service=n8n

      # ---------- Webhooks & Forms for lemonfit.kolberg.uz ----------
       # Form: https://lemonfit.kolberg.uz/form/<path> -> n8n /form/lemonfit/<path>
       - traefik.http.middlewares.n8n-lemonfit-form.replacepathregex.regex=^/form/(.*)$
       - traefik.http.middlewares.n8n-lemonfit-form.replacepathregex.replacement=/form/lemonfit/$1
       - traefik.http.routers.n8n-form-lemonfit.rule=Host(`lemonfit.kolberg.uz`) && PathPrefix(`/form/`)
       - traefik.http.routers.n8n-form-lemonfit.entrypoints=web,websecure
       - traefik.http.routers.n8n-form-lemonfit.tls=true
       - traefik.http.routers.n8n-form-lemonfit.tls.certresolver=mytlschallenge
       - traefik.http.routers.n8n-form-lemonfit.priority=100
       - traefik.http.routers.n8n-form-lemonfit.middlewares=n8n-lemonfit-form@docker,n8n@docker,n8n-error-pages@docker
       - traefik.http.routers.n8n-form-lemonfit.service=n8n

       # Webhook prefix: https://lemonfit.kolberg.uz/webhook/<path> -> n8n /webhook/lemonfit/<path>
       - traefik.http.middlewares.n8n-lemonfit-whp.replacepathregex.regex=^/webhook/(.*)$
       - traefik.http.middlewares.n8n-lemonfit-whp.replacepathregex.replacement=/webhook/lemonfit/$1
       - traefik.http.routers.n8n-whp-lemonfit.rule=Host(`lemonfit.kolberg.uz`) && PathPrefix(`/webhook/`)
       - traefik.http.routers.n8n-whp-lemonfit.entrypoints=web,websecure
       - traefik.http.routers.n8n-whp-lemonfit.tls=true
       - traefik.http.routers.n8n-whp-lemonfit.tls.certresolver=mytlschallenge
       - traefik.http.routers.n8n-whp-lemonfit.priority=100
       - traefik.http.routers.n8n-whp-lemonfit.middlewares=n8n-lemonfit-whp@docker,n8n@docker,n8n-error-pages@docker
       - traefik.http.routers.n8n-whp-lemonfit.service=n8n

       # Short paths: https://lemonfit.kolberg.uz/<path> -> n8n /webhook/lemonfit/<path>
       - traefik.http.middlewares.n8n-lemonfit-webhook.replacepathregex.regex=^/(.*)$
       - traefik.http.middlewares.n8n-lemonfit-webhook.replacepathregex.replacement=/webhook/lemonfit/$1
       - traefik.http.routers.n8n-webhook-lemonfit.rule=Host(`lemonfit.kolberg.uz`)
       - traefik.http.routers.n8n-webhook-lemonfit.priority=1
       - traefik.http.routers.n8n-webhook-lemonfit.entrypoints=web,websecure
       - traefik.http.routers.n8n-webhook-lemonfit.tls=true
       - traefik.http.routers.n8n-webhook-lemonfit.tls.certresolver=mytlschallenge
       - traefik.http.routers.n8n-webhook-lemonfit.middlewares=n8n-lemonfit-webhook@docker,n8n@docker,n8n-error-pages@docker
       - traefik.http.routers.n8n-webhook-lemonfit.service=n8n

      # ---------- Webhooks & Forms for construction.kolberg.uz ----------
       # Form: https://construction.kolberg.uz/form/<path> -> n8n /form/construction/<path>
       - traefik.http.middlewares.n8n-construction-form.replacepathregex.regex=^/form/(.*)$
       - traefik.http.middlewares.n8n-construction-form.replacepathregex.replacement=/form/construction/$1
       - traefik.http.routers.n8n-form-construction.rule=Host(`construction.kolberg.uz`) && PathPrefix(`/form/`)
       - traefik.http.routers.n8n-form-construction.entrypoints=web,websecure
       - traefik.http.routers.n8n-form-construction.tls=true
       - traefik.http.routers.n8n-form-construction.tls.certresolver=mytlschallenge
       - traefik.http.routers.n8n-form-construction.priority=100
       - traefik.http.routers.n8n-form-construction.middlewares=n8n-construction-form@docker,n8n@docker,n8n-error-pages@docker
       - traefik.http.routers.n8n-form-construction.service=n8n

       # Webhook prefix: https://construction.kolberg.uz/webhook/<path> -> n8n /webhook/construction/<path>
       - traefik.http.middlewares.n8n-construction-whp.replacepathregex.regex=^/webhook/(.*)$
       - traefik.http.middlewares.n8n-construction-whp.replacepathregex.replacement=/webhook/construction/$1
       - traefik.http.routers.n8n-whp-construction.rule=Host(`construction.kolberg.uz`) && PathPrefix(`/webhook/`)
       - traefik.http.routers.n8n-whp-construction.entrypoints=web,websecure
       - traefik.http.routers.n8n-whp-construction.tls=true
       - traefik.http.routers.n8n-whp-construction.tls.certresolver=mytlschallenge
       - traefik.http.routers.n8n-whp-construction.priority=100
       - traefik.http.routers.n8n-whp-construction.middlewares=n8n-construction-whp@docker,n8n@docker,n8n-error-pages@docker
       - traefik.http.routers.n8n-whp-construction.service=n8n

       # Bare /request opens the Form workflow path construction/request; does not match /requests-data etc.
       - traefik.http.middlewares.n8n-construction-form-request.replacepath.path=/form/construction/request
       - traefik.http.routers.n8n-form-request-construction.rule=Host(`construction.kolberg.uz`) && PathRegexp(`^/request/?$`)
       - traefik.http.routers.n8n-form-request-construction.entrypoints=web,websecure
       - traefik.http.routers.n8n-form-request-construction.tls=true
       - traefik.http.routers.n8n-form-request-construction.tls.certresolver=mytlschallenge
       - traefik.http.routers.n8n-form-request-construction.priority=100
       - traefik.http.routers.n8n-form-request-construction.middlewares=n8n-construction-form-request@docker,n8n@docker,n8n-error-pages@docker
       - traefik.http.routers.n8n-form-request-construction.service=n8n

       # Short paths: https://construction.kolberg.uz/<path> -> n8n /webhook/construction/<path>
       - traefik.http.middlewares.n8n-construction-webhook.replacepathregex.regex=^/(.*)$
       - traefik.http.middlewares.n8n-construction-webhook.replacepathregex.replacement=/webhook/construction/$1
       - traefik.http.routers.n8n-webhook-construction.rule=Host(`construction.kolberg.uz`)
       - traefik.http.routers.n8n-webhook-construction.priority=1
       - traefik.http.routers.n8n-webhook-construction.entrypoints=web,websecure
       - traefik.http.routers.n8n-webhook-construction.tls=true
       - traefik.http.routers.n8n-webhook-construction.tls.certresolver=mytlschallenge
       - traefik.http.routers.n8n-webhook-construction.middlewares=n8n-construction-webhook@docker,n8n@docker,n8n-error-pages@docker
       - traefik.http.routers.n8n-webhook-construction.service=n8n

      # ---------- Webhooks & Forms for neuron.kolberg.uz ----------
       # Form: https://neuron.kolberg.uz/form/<path> -> n8n /form/neuron/<path>
       - traefik.http.middlewares.n8n-neuron-form.replacepathregex.regex=^/form/(.*)$
       - traefik.http.middlewares.n8n-neuron-form.replacepathregex.replacement=/form/neuron/$1
       - traefik.http.routers.n8n-form-neuron.rule=Host(`neuron.kolberg.uz`) && PathPrefix(`/form/`)
       - traefik.http.routers.n8n-form-neuron.entrypoints=web,websecure
       - traefik.http.routers.n8n-form-neuron.tls=true
       - traefik.http.routers.n8n-form-neuron.tls.certresolver=mytlschallenge
       - traefik.http.routers.n8n-form-neuron.priority=100
       - traefik.http.routers.n8n-form-neuron.middlewares=n8n-neuron-form@docker,n8n@docker,n8n-error-pages@docker
       - traefik.http.routers.n8n-form-neuron.service=n8n

       # Webhook prefix: https://neuron.kolberg.uz/webhook/<path> -> n8n /webhook/neuron/<path>
       - traefik.http.middlewares.n8n-neuron-whp.replacepathregex.regex=^/webhook/(.*)$
       - traefik.http.middlewares.n8n-neuron-whp.replacepathregex.replacement=/webhook/neuron/$1
       - traefik.http.routers.n8n-whp-neuron.rule=Host(`neuron.kolberg.uz`) && PathPrefix(`/webhook/`)
       - traefik.http.routers.n8n-whp-neuron.entrypoints=web,websecure
       - traefik.http.routers.n8n-whp-neuron.tls=true
       - traefik.http.routers.n8n-whp-neuron.tls.certresolver=mytlschallenge
       - traefik.http.routers.n8n-whp-neuron.priority=100
       - traefik.http.routers.n8n-whp-neuron.middlewares=n8n-neuron-whp@docker,n8n@docker,n8n-error-pages@docker
       - traefik.http.routers.n8n-whp-neuron.service=n8n

       # Bare /request opens the Form workflow path neuron/request; does not match /requests-data etc.
       - traefik.http.middlewares.n8n-neuron-form-request.replacepath.path=/form/neuron/request
       - traefik.http.routers.n8n-form-request-neuron.rule=Host(`neuron.kolberg.uz`) && PathRegexp(`^/request/?$`)
       - traefik.http.routers.n8n-form-request-neuron.entrypoints=web,websecure
       - traefik.http.routers.n8n-form-request-neuron.tls=true
       - traefik.http.routers.n8n-form-request-neuron.tls.certresolver=mytlschallenge
       - traefik.http.routers.n8n-form-request-neuron.priority=100
       - traefik.http.routers.n8n-form-request-neuron.middlewares=n8n-neuron-form-request@docker,n8n@docker,n8n-error-pages@docker
       - traefik.http.routers.n8n-form-request-neuron.service=n8n

       # Short paths: https://neuron.kolberg.uz/<path> -> n8n /webhook/neuron/<path>
       - traefik.http.middlewares.n8n-neuron-webhook.replacepathregex.regex=^/(.*)$
       - traefik.http.middlewares.n8n-neuron-webhook.replacepathregex.replacement=/webhook/neuron/$1
       - traefik.http.routers.n8n-webhook-neuron.rule=Host(`neuron.kolberg.uz`)
       - traefik.http.routers.n8n-webhook-neuron.priority=1
       - traefik.http.routers.n8n-webhook-neuron.entrypoints=web,websecure
       - traefik.http.routers.n8n-webhook-neuron.tls=true
       - traefik.http.routers.n8n-webhook-neuron.tls.certresolver=mytlschallenge
       - traefik.http.routers.n8n-webhook-neuron.middlewares=n8n-neuron-webhook@docker,n8n@docker,n8n-error-pages@docker
       - traefik.http.routers.n8n-webhook-neuron.service=n8n

      # ---------- Webhooks & Forms for lemonfit2.kolberg.uz ----------
       # Form: https://lemonfit2.kolberg.uz/form/<path> -> n8n /form/lemonfit2/<path>
       - traefik.http.middlewares.n8n-lemonfit2-form.replacepathregex.regex=^/form/(.*)$
       - traefik.http.middlewares.n8n-lemonfit2-form.replacepathregex.replacement=/form/lemonfit2/$1
       - traefik.http.routers.n8n-form-lemonfit2.rule=Host(`lemonfit2.kolberg.uz`) && PathPrefix(`/form/`)
       - traefik.http.routers.n8n-form-lemonfit2.entrypoints=web,websecure
       - traefik.http.routers.n8n-form-lemonfit2.tls=true
       - traefik.http.routers.n8n-form-lemonfit2.tls.certresolver=mytlschallenge
       - traefik.http.routers.n8n-form-lemonfit2.priority=100
       - traefik.http.routers.n8n-form-lemonfit2.middlewares=n8n-lemonfit2-form@docker,n8n@docker,n8n-error-pages@docker
       - traefik.http.routers.n8n-form-lemonfit2.service=n8n

       # Webhook prefix: https://lemonfit2.kolberg.uz/webhook/<path> -> n8n /webhook/lemonfit2/<path>
       - traefik.http.middlewares.n8n-lemonfit2-whp.replacepathregex.regex=^/webhook/(.*)$
       - traefik.http.middlewares.n8n-lemonfit2-whp.replacepathregex.replacement=/webhook/lemonfit2/$1
       - traefik.http.routers.n8n-whp-lemonfit2.rule=Host(`lemonfit2.kolberg.uz`) && PathPrefix(`/webhook/`)
       - traefik.http.routers.n8n-whp-lemonfit2.entrypoints=web,websecure
       - traefik.http.routers.n8n-whp-lemonfit2.tls=true
       - traefik.http.routers.n8n-whp-lemonfit2.tls.certresolver=mytlschallenge
       - traefik.http.routers.n8n-whp-lemonfit2.priority=100
       - traefik.http.routers.n8n-whp-lemonfit2.middlewares=n8n-lemonfit2-whp@docker,n8n@docker,n8n-error-pages@docker
       - traefik.http.routers.n8n-whp-lemonfit2.service=n8n

       # Short paths: https://lemonfit2.kolberg.uz/<path> -> n8n /webhook/lemonfit2/<path>
       - traefik.http.middlewares.n8n-lemonfit2-webhook.replacepathregex.regex=^/(.*)$
       - traefik.http.middlewares.n8n-lemonfit2-webhook.replacepathregex.replacement=/webhook/lemonfit2/$1
       - traefik.http.routers.n8n-webhook-lemonfit2.rule=Host(`lemonfit2.kolberg.uz`)
       - traefik.http.routers.n8n-webhook-lemonfit2.priority=1
       - traefik.http.routers.n8n-webhook-lemonfit2.entrypoints=web,websecure
       - traefik.http.routers.n8n-webhook-lemonfit2.tls=true
       - traefik.http.routers.n8n-webhook-lemonfit2.tls.certresolver=mytlschallenge
       - traefik.http.routers.n8n-webhook-lemonfit2.middlewares=n8n-lemonfit2-webhook@docker,n8n@docker,n8n-error-pages@docker
       - traefik.http.routers.n8n-webhook-lemonfit2.service=n8n

      # ---------- Webhooks & Forms for lemonaqua.kolberg.uz ----------
       # Form: https://lemonaqua.kolberg.uz/form/<path> -> n8n /form/lemonaqua/<path>
       - traefik.http.middlewares.n8n-lemonaqua-form.replacepathregex.regex=^/form/(.*)$
       - traefik.http.middlewares.n8n-lemonaqua-form.replacepathregex.replacement=/form/lemonaqua/$1
       - traefik.http.routers.n8n-form-lemonaqua.rule=Host(`lemonaqua.kolberg.uz`) && PathPrefix(`/form/`)
       - traefik.http.routers.n8n-form-lemonaqua.entrypoints=web,websecure
       - traefik.http.routers.n8n-form-lemonaqua.tls=true
       - traefik.http.routers.n8n-form-lemonaqua.tls.certresolver=mytlschallenge
       - traefik.http.routers.n8n-form-lemonaqua.priority=100
       - traefik.http.routers.n8n-form-lemonaqua.middlewares=n8n-lemonaqua-form@docker,n8n@docker,n8n-error-pages@docker
       - traefik.http.routers.n8n-form-lemonaqua.service=n8n

       # Webhook prefix: https://lemonaqua.kolberg.uz/webhook/<path> -> n8n /webhook/lemonaqua/<path>
       - traefik.http.middlewares.n8n-lemonaqua-whp.replacepathregex.regex=^/webhook/(.*)$
       - traefik.http.middlewares.n8n-lemonaqua-whp.replacepathregex.replacement=/webhook/lemonaqua/$1
       - traefik.http.routers.n8n-whp-lemonaqua.rule=Host(`lemonaqua.kolberg.uz`) && PathPrefix(`/webhook/`)
       - traefik.http.routers.n8n-whp-lemonaqua.entrypoints=web,websecure
       - traefik.http.routers.n8n-whp-lemonaqua.tls=true
       - traefik.http.routers.n8n-whp-lemonaqua.tls.certresolver=mytlschallenge
       - traefik.http.routers.n8n-whp-lemonaqua.priority=100
       - traefik.http.routers.n8n-whp-lemonaqua.middlewares=n8n-lemonaqua-whp@docker,n8n@docker,n8n-error-pages@docker
       - traefik.http.routers.n8n-whp-lemonaqua.service=n8n

       # Short paths: https://lemonaqua.kolberg.uz/<path> -> n8n /webhook/lemonaqua/<path>
       - traefik.http.middlewares.n8n-lemonaqua-webhook.replacepathregex.regex=^/(.*)$
       - traefik.http.middlewares.n8n-lemonaqua-webhook.replacepathregex.replacement=/webhook/lemonaqua/$1
       - traefik.http.routers.n8n-webhook-lemonaqua.rule=Host(`lemonaqua.kolberg.uz`)
       - traefik.http.routers.n8n-webhook-lemonaqua.priority=1
       - traefik.http.routers.n8n-webhook-lemonaqua.entrypoints=web,websecure
       - traefik.http.routers.n8n-webhook-lemonaqua.tls=true
       - traefik.http.routers.n8n-webhook-lemonaqua.tls.certresolver=mytlschallenge
       - traefik.http.routers.n8n-webhook-lemonaqua.middlewares=n8n-lemonaqua-webhook@docker,n8n@docker,n8n-error-pages@docker
       - traefik.http.routers.n8n-webhook-lemonaqua.service=n8n

      # ---------- Webhooks & Forms for main.kolberg.uz ----------
       # Form: https://main.kolberg.uz/form/<path> -> n8n /form/main/<path>
       - traefik.http.middlewares.n8n-main-form.replacepathregex.regex=^/form/(.*)$
       - traefik.http.middlewares.n8n-main-form.replacepathregex.replacement=/form/main/$1
       - traefik.http.routers.n8n-form-main.rule=Host(`main.kolberg.uz`) && PathPrefix(`/form/`)
       - traefik.http.routers.n8n-form-main.entrypoints=web,websecure
       - traefik.http.routers.n8n-form-main.tls=true
       - traefik.http.routers.n8n-form-main.tls.certresolver=mytlschallenge
       - traefik.http.routers.n8n-form-main.priority=100
       - traefik.http.routers.n8n-form-main.middlewares=n8n-main-form@docker,n8n@docker,n8n-error-pages@docker
       - traefik.http.routers.n8n-form-main.service=n8n

       # Webhook prefix: https://main.kolberg.uz/webhook/<path> -> n8n /webhook/main/<path>
       - traefik.http.middlewares.n8n-main-whp.replacepathregex.regex=^/webhook/(.*)$
       - traefik.http.middlewares.n8n-main-whp.replacepathregex.replacement=/webhook/main/$1
       - traefik.http.routers.n8n-whp-main.rule=Host(`main.kolberg.uz`) && PathPrefix(`/webhook/`)
       - traefik.http.routers.n8n-whp-main.entrypoints=web,websecure
       - traefik.http.routers.n8n-whp-main.tls=true
       - traefik.http.routers.n8n-whp-main.tls.certresolver=mytlschallenge
       - traefik.http.routers.n8n-whp-main.priority=100
       - traefik.http.routers.n8n-whp-main.middlewares=n8n-main-whp@docker,n8n@docker,n8n-error-pages@docker
       - traefik.http.routers.n8n-whp-main.service=n8n

       # Short paths: https://main.kolberg.uz/<path> -> n8n /webhook/main/<path>
       - traefik.http.middlewares.n8n-main-webhook.replacepathregex.regex=^/(.*)$
       - traefik.http.middlewares.n8n-main-webhook.replacepathregex.replacement=/webhook/main/$1
       - traefik.http.routers.n8n-webhook-main.rule=Host(`main.kolberg.uz`)
       - traefik.http.routers.n8n-webhook-main.priority=1
       - traefik.http.routers.n8n-webhook-main.entrypoints=web,websecure
       - traefik.http.routers.n8n-webhook-main.tls=true
       - traefik.http.routers.n8n-webhook-main.tls.certresolver=mytlschallenge
       - traefik.http.routers.n8n-webhook-main.middlewares=n8n-main-webhook@docker,n8n@docker,n8n-error-pages@docker
       - traefik.http.routers.n8n-webhook-main.service=n8n

      # ---------- Webhooks & Forms for zarqand.kolberg.uz ----------
       # Form: https://zarqand.kolberg.uz/form/<path> -> n8n /form/zarqand/<path>
       - traefik.http.middlewares.n8n-zarqand-form.replacepathregex.regex=^/form/(.*)$
       - traefik.http.middlewares.n8n-zarqand-form.replacepathregex.replacement=/form/zarqand/$1
       - traefik.http.routers.n8n-form-zarqand.rule=Host(`zarqand.kolberg.uz`) && PathPrefix(`/form/`)
       - traefik.http.routers.n8n-form-zarqand.entrypoints=web,websecure
       - traefik.http.routers.n8n-form-zarqand.tls=true
       - traefik.http.routers.n8n-form-zarqand.tls.certresolver=mytlschallenge
       - traefik.http.routers.n8n-form-zarqand.priority=100
       - traefik.http.routers.n8n-form-zarqand.middlewares=n8n-zarqand-form@docker,n8n@docker,n8n-error-pages@docker
       - traefik.http.routers.n8n-form-zarqand.service=n8n

       # Webhook prefix: https://zarqand.kolberg.uz/webhook/<path> -> n8n /webhook/zarqand/<path>
       - traefik.http.middlewares.n8n-zarqand-whp.replacepathregex.regex=^/webhook/(.*)$
       - traefik.http.middlewares.n8n-zarqand-whp.replacepathregex.replacement=/webhook/zarqand/$1
       - traefik.http.routers.n8n-whp-zarqand.rule=Host(`zarqand.kolberg.uz`) && PathPrefix(`/webhook/`)
       - traefik.http.routers.n8n-whp-zarqand.entrypoints=web,websecure
       - traefik.http.routers.n8n-whp-zarqand.tls=true
       - traefik.http.routers.n8n-whp-zarqand.tls.certresolver=mytlschallenge
       - traefik.http.routers.n8n-whp-zarqand.priority=100
       - traefik.http.routers.n8n-whp-zarqand.middlewares=n8n-zarqand-whp@docker,n8n@docker,n8n-error-pages@docker
       - traefik.http.routers.n8n-whp-zarqand.service=n8n

       # Short paths: https://zarqand.kolberg.uz/<path> -> n8n /webhook/zarqand/<path>
       - traefik.http.middlewares.n8n-zarqand-webhook.replacepathregex.regex=^/(.*)$
       - traefik.http.middlewares.n8n-zarqand-webhook.replacepathregex.replacement=/webhook/zarqand/$1
       - traefik.http.routers.n8n-webhook-zarqand.rule=Host(`zarqand.kolberg.uz`)
       - traefik.http.routers.n8n-webhook-zarqand.priority=1
       - traefik.http.routers.n8n-webhook-zarqand.entrypoints=web,websecure
       - traefik.http.routers.n8n-webhook-zarqand.tls=true
       - traefik.http.routers.n8n-webhook-zarqand.tls.certresolver=mytlschallenge
       - traefik.http.routers.n8n-webhook-zarqand.middlewares=n8n-zarqand-webhook@docker,n8n@docker,n8n-error-pages@docker
       - traefik.http.routers.n8n-webhook-zarqand.service=n8n

      # ---------- Webhooks & Forms for invest.kolberg.uz ----------
       # Form: https://invest.kolberg.uz/form/<path> -> n8n /form/invest/<path>
       - traefik.http.middlewares.n8n-invest-form.replacepathregex.regex=^/form/(.*)$
       - traefik.http.middlewares.n8n-invest-form.replacepathregex.replacement=/form/invest/$1
       - traefik.http.routers.n8n-form-invest.rule=Host(`invest.kolberg.uz`) && PathPrefix(`/form/`)
       - traefik.http.routers.n8n-form-invest.entrypoints=web,websecure
       - traefik.http.routers.n8n-form-invest.tls=true
       - traefik.http.routers.n8n-form-invest.tls.certresolver=mytlschallenge
       - traefik.http.routers.n8n-form-invest.priority=100
       - traefik.http.routers.n8n-form-invest.middlewares=n8n-invest-form@docker,n8n@docker,n8n-error-pages@docker
       - traefik.http.routers.n8n-form-invest.service=n8n

       # Webhook prefix: https://invest.kolberg.uz/webhook/<path> -> n8n /webhook/invest/<path>
       - traefik.http.middlewares.n8n-invest-whp.replacepathregex.regex=^/webhook/(.*)$
       - traefik.http.middlewares.n8n-invest-whp.replacepathregex.replacement=/webhook/invest/$1
       - traefik.http.routers.n8n-whp-invest.rule=Host(`invest.kolberg.uz`) && PathPrefix(`/webhook/`)
       - traefik.http.routers.n8n-whp-invest.entrypoints=web,websecure
       - traefik.http.routers.n8n-whp-invest.tls=true
       - traefik.http.routers.n8n-whp-invest.tls.certresolver=mytlschallenge
       - traefik.http.routers.n8n-whp-invest.priority=100
       - traefik.http.routers.n8n-whp-invest.middlewares=n8n-invest-whp@docker,n8n@docker,n8n-error-pages@docker
       - traefik.http.routers.n8n-whp-invest.service=n8n

       # Short paths: https://invest.kolberg.uz/<path> -> n8n /webhook/invest/<path>
       - traefik.http.middlewares.n8n-invest-webhook.replacepathregex.regex=^/(.*)$
       - traefik.http.middlewares.n8n-invest-webhook.replacepathregex.replacement=/webhook/invest/$1
       - traefik.http.routers.n8n-webhook-invest.rule=Host(`invest.kolberg.uz`)
       - traefik.http.routers.n8n-webhook-invest.priority=1
       - traefik.http.routers.n8n-webhook-invest.entrypoints=web,websecure
       - traefik.http.routers.n8n-webhook-invest.tls=true
       - traefik.http.routers.n8n-webhook-invest.tls.certresolver=mytlschallenge
       - traefik.http.routers.n8n-webhook-invest.middlewares=n8n-invest-webhook@docker,n8n@docker,n8n-error-pages@docker
       - traefik.http.routers.n8n-webhook-invest.service=n8n

      # ---------- Webhooks & Forms for syrop.kolberg.uz ----------
       # Form: https://syrop.kolberg.uz/form/<path> -> n8n /form/syrop/<path>
       - traefik.http.middlewares.n8n-syrop-form.replacepathregex.regex=^/form/(.*)$
       - traefik.http.middlewares.n8n-syrop-form.replacepathregex.replacement=/form/syrop/$1
       - traefik.http.routers.n8n-form-syrop.rule=Host(`syrop.kolberg.uz`) && PathPrefix(`/form/`)
       - traefik.http.routers.n8n-form-syrop.entrypoints=web,websecure
       - traefik.http.routers.n8n-form-syrop.tls=true
       - traefik.http.routers.n8n-form-syrop.tls.certresolver=mytlschallenge
       - traefik.http.routers.n8n-form-syrop.priority=100
       - traefik.http.routers.n8n-form-syrop.middlewares=n8n-syrop-form@docker,n8n@docker,n8n-error-pages@docker
       - traefik.http.routers.n8n-form-syrop.service=n8n

       # Webhook prefix: https://syrop.kolberg.uz/webhook/<path> -> n8n /webhook/syrop/<path>
       - traefik.http.middlewares.n8n-syrop-whp.replacepathregex.regex=^/webhook/(.*)$
       - traefik.http.middlewares.n8n-syrop-whp.replacepathregex.replacement=/webhook/syrop/$1
       - traefik.http.routers.n8n-whp-syrop.rule=Host(`syrop.kolberg.uz`) && PathPrefix(`/webhook/`)
       - traefik.http.routers.n8n-whp-syrop.entrypoints=web,websecure
       - traefik.http.routers.n8n-whp-syrop.tls=true
       - traefik.http.routers.n8n-whp-syrop.tls.certresolver=mytlschallenge
       - traefik.http.routers.n8n-whp-syrop.priority=100
       - traefik.http.routers.n8n-whp-syrop.middlewares=n8n-syrop-whp@docker,n8n@docker,n8n-error-pages@docker
       - traefik.http.routers.n8n-whp-syrop.service=n8n

       # Short paths: https://syrop.kolberg.uz/<path> -> n8n /webhook/syrop/<path>
       - traefik.http.middlewares.n8n-syrop-webhook.replacepathregex.regex=^/(.*)$
       - traefik.http.middlewares.n8n-syrop-webhook.replacepathregex.replacement=/webhook/syrop/$1
       - traefik.http.routers.n8n-webhook-syrop.rule=Host(`syrop.kolberg.uz`)
       - traefik.http.routers.n8n-webhook-syrop.priority=1
       - traefik.http.routers.n8n-webhook-syrop.entrypoints=web,websecure
       - traefik.http.routers.n8n-webhook-syrop.tls=true
       - traefik.http.routers.n8n-webhook-syrop.tls.certresolver=mytlschallenge
       - traefik.http.routers.n8n-webhook-syrop.middlewares=n8n-syrop-webhook@docker,n8n@docker,n8n-error-pages@docker
       - traefik.http.routers.n8n-webhook-syrop.service=n8n

      # ---------- Webhooks & Forms for qulay.kolberg.uz ----------
       # Form: https://qulay.kolberg.uz/form/<path> -> n8n /form/qulay/<path>
       - traefik.http.middlewares.n8n-qulay-form.replacepathregex.regex=^/form/(.*)$
       - traefik.http.middlewares.n8n-qulay-form.replacepathregex.replacement=/form/qulay/$1
       - traefik.http.routers.n8n-form-qulay.rule=Host(`qulay.kolberg.uz`) && PathPrefix(`/form/`)
       - traefik.http.routers.n8n-form-qulay.entrypoints=web,websecure
       - traefik.http.routers.n8n-form-qulay.tls=true
       - traefik.http.routers.n8n-form-qulay.tls.certresolver=mytlschallenge
       - traefik.http.routers.n8n-form-qulay.priority=100
       - traefik.http.routers.n8n-form-qulay.middlewares=n8n-qulay-form@docker,n8n@docker,n8n-error-pages@docker
       - traefik.http.routers.n8n-form-qulay.service=n8n

       # Webhook prefix: https://qulay.kolberg.uz/webhook/<path> -> n8n /webhook/qulay/<path>
       - traefik.http.middlewares.n8n-qulay-whp.replacepathregex.regex=^/webhook/(.*)$
       - traefik.http.middlewares.n8n-qulay-whp.replacepathregex.replacement=/webhook/qulay/$1
       - traefik.http.routers.n8n-whp-qulay.rule=Host(`qulay.kolberg.uz`) && PathPrefix(`/webhook/`)
       - traefik.http.routers.n8n-whp-qulay.entrypoints=web,websecure
       - traefik.http.routers.n8n-whp-qulay.tls=true
       - traefik.http.routers.n8n-whp-qulay.tls.certresolver=mytlschallenge
       - traefik.http.routers.n8n-whp-qulay.priority=100
       - traefik.http.routers.n8n-whp-qulay.middlewares=n8n-qulay-whp@docker,n8n@docker,n8n-error-pages@docker
       - traefik.http.routers.n8n-whp-qulay.service=n8n

       # Short paths: https://qulay.kolberg.uz/<path> -> n8n /webhook/qulay/<path>
       - traefik.http.middlewares.n8n-qulay-webhook.replacepathregex.regex=^/(.*)$
       - traefik.http.middlewares.n8n-qulay-webhook.replacepathregex.replacement=/webhook/qulay/$1
       - traefik.http.routers.n8n-webhook-qulay.rule=Host(`qulay.kolberg.uz`)
       - traefik.http.routers.n8n-webhook-qulay.priority=1
       - traefik.http.routers.n8n-webhook-qulay.entrypoints=web,websecure
       - traefik.http.routers.n8n-webhook-qulay.tls=true
       - traefik.http.routers.n8n-webhook-qulay.tls.certresolver=mytlschallenge
       - traefik.http.routers.n8n-webhook-qulay.middlewares=n8n-qulay-webhook@docker,n8n@docker,n8n-error-pages@docker
       - traefik.http.routers.n8n-webhook-qulay.service=n8n

      # ---------- Webhooks & Forms for lemonelectro.kolberg.uz ----------
       # Form: https://lemonelectro.kolberg.uz/form/<path> -> n8n /form/lemonelectro/<path>
       - traefik.http.middlewares.n8n-lemonelectro-form.replacepathregex.regex=^/form/(.*)$
       - traefik.http.middlewares.n8n-lemonelectro-form.replacepathregex.replacement=/form/lemonelectro/$1
       - traefik.http.routers.n8n-form-lemonelectro.rule=Host(`lemonelectro.kolberg.uz`) && PathPrefix(`/form/`)
       - traefik.http.routers.n8n-form-lemonelectro.entrypoints=web,websecure
       - traefik.http.routers.n8n-form-lemonelectro.tls=true
       - traefik.http.routers.n8n-form-lemonelectro.tls.certresolver=mytlschallenge
       - traefik.http.routers.n8n-form-lemonelectro.priority=100
       - traefik.http.routers.n8n-form-lemonelectro.middlewares=n8n-lemonelectro-form@docker,n8n@docker,n8n-error-pages@docker
       - traefik.http.routers.n8n-form-lemonelectro.service=n8n

       # Webhook prefix: https://lemonelectro.kolberg.uz/webhook/<path> -> n8n /webhook/lemonelectro/<path>
       - traefik.http.middlewares.n8n-lemonelectro-whp.replacepathregex.regex=^/webhook/(.*)$
       - traefik.http.middlewares.n8n-lemonelectro-whp.replacepathregex.replacement=/webhook/lemonelectro/$1
       - traefik.http.routers.n8n-whp-lemonelectro.rule=Host(`lemonelectro.kolberg.uz`) && PathPrefix(`/webhook/`)
       - traefik.http.routers.n8n-whp-lemonelectro.entrypoints=web,websecure
       - traefik.http.routers.n8n-whp-lemonelectro.tls=true
       - traefik.http.routers.n8n-whp-lemonelectro.tls.certresolver=mytlschallenge
       - traefik.http.routers.n8n-whp-lemonelectro.priority=100
       - traefik.http.routers.n8n-whp-lemonelectro.middlewares=n8n-lemonelectro-whp@docker,n8n@docker,n8n-error-pages@docker
       - traefik.http.routers.n8n-whp-lemonelectro.service=n8n

       # Short paths: https://lemonelectro.kolberg.uz/<path> -> n8n /webhook/lemonelectro/<path>
       - traefik.http.middlewares.n8n-lemonelectro-webhook.replacepathregex.regex=^/(.*)$
       - traefik.http.middlewares.n8n-lemonelectro-webhook.replacepathregex.replacement=/webhook/lemonelectro/$1
       - traefik.http.routers.n8n-webhook-lemonelectro.rule=Host(`lemonelectro.kolberg.uz`)
       - traefik.http.routers.n8n-webhook-lemonelectro.priority=1
       - traefik.http.routers.n8n-webhook-lemonelectro.entrypoints=web,websecure
       - traefik.http.routers.n8n-webhook-lemonelectro.tls=true
       - traefik.http.routers.n8n-webhook-lemonelectro.tls.certresolver=mytlschallenge
       - traefik.http.routers.n8n-webhook-lemonelectro.middlewares=n8n-lemonelectro-webhook@docker,n8n@docker,n8n-error-pages@docker
       - traefik.http.routers.n8n-webhook-lemonelectro.service=n8n

      # ---------- Final n8n service config ----------
       - traefik.http.services.n8n.loadbalancer.server.port=5678

    environment:
      - N8N_ENFORCE_SETTINGS_FILE_PERMISSIONS=true
      - N8N_HOST=dev.kolberg.uz
      - N8N_PORT=5678
      - N8N_PROTOCOL=https
      - N8N_RUNNERS_ENABLED=true
      - NODE_ENV=production
      - WEBHOOK_URL=https://dev.kolberg.uz
      - GENERIC_TIMEZONE=${GENERIC_TIMEZONE}
      - TZ=${GENERIC_TIMEZONE}
      - N8N_CONCURRENCY_PRODUCTION_LIMIT=10
      - N8N_PROXY_HOPS=1

      - N8N_DEFAULT_CORS=true
      - N8N_CORS_ALLOW_ORIGIN=https://neuron.kolberg.uz,https://construction.kolberg.uz,https://lemonfit.kolberg.uz,https://lemonaqua.kolberg.uz,https://main.kolberg.uz

      - EXECUTIONS_DATA_PRUNE=true
      - EXECUTIONS_DATA_MAX_AGE=720
      - EXECUTIONS_DATA_PRUNE_MAX_COUNT=10000
      - EXECUTIONS_TIMEOUT=300
      - EXECUTIONS_TIMEOUT_MAX=432000

      - DB_TYPE=postgresdb
      - DB_POSTGRESDB_HOST=db
      - DB_POSTGRESDB_PORT=5432
      - DB_POSTGRESDB_DATABASE=${POSTGRES_N8N_DB}
      - DB_POSTGRESDB_USER=${POSTGRES_N8N_USER}
      - DB_POSTGRESDB_PASSWORD=${POSTGRES_N8N_PASSWORD}
      - DB_POSTGRESDB_SCHEMA=public
      - N8N_LOG_LEVEL=debug

    volumes:
      - n8n_data:/home/node/.n8n
      - ./local-files:/files
    # >>> CHANGE: n8n в public (для trmakeaefik) и backend (т.к. ходит в db)
    networks:
      - traefik-public
      - backend
      - chromium

  error-pages:
    image: nginx:alpine
    container_name: error-pages
    restart: always
    volumes:
      - ./error-pages:/usr/share/nginx/html:ro
    labels:
      - traefik.enable=true
      - traefik.docker.network=n8n_traefik-public
      - traefik.http.services.error-pages.loadbalancer.server.port=80

      # Serve custom pages for 404 and 409
      - traefik.http.middlewares.n8n-error-pages.errors.status=404,409
      - traefik.http.middlewares.n8n-error-pages.errors.service=error-pages
      - traefik.http.middlewares.n8n-error-pages.errors.query=/{status}.html
    # >>> CHANGE: только public
    networks:
      - traefik-public

  db_backup:
    image: postgres:17
    container_name: db_backup
    restart: always
    depends_on:
      - db
    environment:
      POSTGRES_USER: ${POSTGRES_USER}
      PGPASSWORD: ${POSTGRES_PASSWORD}
    volumes:
      - /opt/backups/postgres:/backups
    command: >
      bash -c "
      while true; do
        now_hm=$$(date +%H:%M) ;
        today=$$(date +%F) ;
        last_backup=$$(cat /tmp/last_db_backup_date 2>/dev/null || true) ;
        if [ \"$$now_hm\" = \"01:00\" ] && [ \"$$today\" != \"$$last_backup\" ]; then
          ts=$$(date +%F-%H%M) ;
          pg_dumpall -h db -U \"$POSTGRES_USER\" | gzip > /backups/postgres-all-$$ts.sql.gz ;
          find /backups -type f -name 'postgres-all-*.sql.gz' -mtime +10 -delete ;
          echo \"$$today\" > /tmp/last_db_backup_date ;
        fi ;
        sleep 30 ;
      done"
    # >>> CHANGE: backup только backend
    networks:
      - backend

  # RCLONE_CONFIG_DIR: каталог с rclone.conf на хосте (внутри → /config/rclone).
  # Для rclone из Snap: /home/USER/snap/rclone/current/.config/rclone — «current» всегда на актуальную ревизию, reconnect обновляет тот же путь.
  # Задать в .env, например: RCLONE_CONFIG_DIR=/home/ddavlet/snap/rclone/current/.config/rclone
  # n8n в Postgres (см. сервис n8n: DB_*) — попадает в pg_dumpall у db_backup; отдельные tar больше не синкаем.
  gdrive_backup_sync:
    image: rclone/rclone:latest
    container_name: gdrive_backup_sync
    restart: always
    # Локально db_backup держит ~10 дней; в Drive — только последние GDRIVE_RETENTION_DAYS дней
    # (upload по mtime + purge по min-age на remote). ~2.2–2.6 ГБ на дамп PG.
    environment:
      - GDRIVE_RETENTION_DAYS=3
    entrypoint:
      - /bin/sh
      - -lc
      - |
        while true; do
          now_hm=$$(date +%H:%M);
          today=$$(date +%F);
          last_sync=$$(cat /tmp/last_sync_date 2>/dev/null || true);
          if [ "$$now_hm" = "02:00" ] && [ "$$today" != "$$last_sync" ]; then
            latest_pg=$$(ls -1t /backups/postgres/postgres-all-*.sql.gz 2>/dev/null | sed -n '1p');
            if [ -n "$$latest_pg" ]; then
              rclone copyto "$$latest_pg" gdrive:kolberg-backups/postgres-latest.sql.gz;
              find /backups/postgres -maxdepth 1 -type f -name 'postgres-all-*.sql.gz' -mtime -$$((GDRIVE_RETENTION_DAYS + 1)) 2>/dev/null | while IFS= read -r f; do
                d=$$(basename "$$f" | sed -n 's/postgres-all-\([0-9][0-9][0-9][0-9]-[0-9][0-9]-[0-9][0-9]\)-.*/\1/p');
                [ -n "$$d" ] || continue;
                rclone copyto "$$f" "gdrive:kolberg-backups/postgres-daily/$${d}.sql.gz";
              done;
              rclone delete "gdrive:kolberg-backups/postgres-daily/" --include "*.sql.gz" --min-age "$${GDRIVE_RETENTION_DAYS}d" || true;
            fi;
            echo "$$today" > /tmp/last_sync_date;
          fi;
          sleep 30;
        done
    volumes:
      - /opt/backups/postgres:/backups/postgres:ro
      - ${RCLONE_CONFIG_DIR:-/opt/backups/rclone}:/config/rclone:ro

  portainer:
    image: portainer/portainer-ce:latest
    container_name: portainer
    restart: always
    security_opt:
      - no-new-privileges:true
    volumes:
      - /var/run/docker.sock:/var/run/docker.sock
      - portainer_data:/data
    labels:
      - traefik.enable=true
      - traefik.docker.network=n8n_traefik-public

      - traefik.http.routers.portainer.rule=Host(`docker.kolberg.uz`)
      - traefik.http.routers.portainer.entrypoints=web,websecure
      - traefik.http.routers.portainer.tls=true
      - traefik.http.routers.portainer.tls.certresolver=mytlschallenge

      - traefik.http.services.portainer.loadbalancer.server.port=9000
    networks:
      - traefik-public

  backend:
    build: ./backend
    container_name: django
    restart: always
    environment:
      POSTGRES_DB: ${POSTGRES_WEB_DB}
      POSTGRES_USER: ${POSTGRES_WEB_USER}
      POSTGRES_PASSWORD: ${POSTGRES_WEB_PASSWORD}
      POSTGRES_HOST: "db"
      POSTGRES_PORT: 5432
      DJANGO_SECRET_KEY: ${DJANGO_SECRET_KEY:-change-me-to-a-long-random-secret-key-32-plus-bytes}
      DJANGO_DEBUG: "False"
      N8N_TOKEN: ${N8N_TOKEN}
      CORPORATE_INVESTMENTS_ALLOWED_USER: ddavlet, r.davletyarov
    volumes:
      - ./backend:/app
    depends_on:
      - db
    command: >
      sh -c "
      python manage.py migrate &&
      gunicorn config.wsgi:application --bind 0.0.0.0:8000
      "
    labels:
      - traefik.enable=true
      - traefik.docker.network=n8n_traefik-public
      - traefik.http.services.django.loadbalancer.server.port=8000

      # login.kolberg.uz (оставляем как есть)
      - traefik.http.routers.django-login.rule=Host(`login.kolberg.uz`)
      - traefik.http.routers.django-login.entrypoints=web,websecure
      - traefik.http.routers.django-login.tls=true
      - traefik.http.routers.django-login.tls.certresolver=mytlschallenge
      - traefik.http.routers.django-login.service=django
      - traefik.http.routers.django-login.priority=1000

      # tenants: только /web/*
      - traefik.http.routers.django-web.rule=(Host(`lemonfit.kolberg.uz`) || Host(`lemonfit2.kolberg.uz`) || Host(`neuron.kolberg.uz`)) && PathPrefix(`/web/`)
      - traefik.http.routers.django-web.entrypoints=web,websecure
      - traefik.http.routers.django-web.tls=true
      - traefik.http.routers.django-web.tls.certresolver=mytlschallenge
      - traefik.http.routers.django-web.service=django
      - traefik.http.routers.django-web.priority=1000

    networks:
      - traefik-public
      - backend

  backend_v2:
    build: ./backend_v2
    container_name: django_v2
    restart: always
    environment:
      # Separate DB creds so v2 doesn't share auth tables with v1
      POSTGRES_DB: ${POSTGRES_V2_DB}
      POSTGRES_USER: ${POSTGRES_V2_USER}
      POSTGRES_PASSWORD: ${POSTGRES_V2_PASSWORD}
      POSTGRES_HOST: "db"
      POSTGRES_PORT: 5432

      BASE_DOMAIN: ${DOMAIN_NAME}
      DJANGO_SECRET_KEY: ${DJANGO_SECRET_KEY:-change-me-to-a-long-random-secret-key-32-plus-bytes}
      # Override in `.env` next to this file: DJANGO_DEBUG=true (never leave on in production).
      DJANGO_DEBUG: "${DJANGO_DEBUG:-False}"
      DJANGO_ALLOWED_HOSTS: "main.kolberg.uz,lemonfit.kolberg.uz,lemonfit2.kolberg.uz,neuron.kolberg.uz,lemonaqua.kolberg.uz,qulay.kolberg.uz,lemonelectro.kolberg.uz"
      # No leading/trailing slashes (e.g. n8n). Slashes break Traefik labels: /api/${VAR}/ → /api/n8n//
      N8N_INTEGRATION_URL_PATH: ${N8N_INTEGRATION_URL_PATH:-n8n}
      N8N_INTEGRATION_TOKEN: ${N8N_INTEGRATION_TOKEN:-}
      # Internal n8n webhook base; bypasses Traefik+TLS so report proxies stop paying the public-hop cost.
      # Unset in .env to fall back to https://{subdomain}.{BASE_DOMAIN}.
      N8N_INTERNAL_BASE_URL: ${N8N_INTERNAL_BASE_URL:-http://n8n:5678/webhook}
      N8N_TOKEN: ${N8N_TOKEN:-}
      MESSAGING_GATEWAY_SEND_URL: http://tg_gateway:8080/v1/messaging/send
      MESSAGING_GATEWAY_ADMIN_URL: http://tg_gateway:8080
    volumes:
      - ./backend_v2:/app
    depends_on:
      - db
    command: >
      sh -c "
      python manage.py migrate &&
      gunicorn config.wsgi:application --bind 0.0.0.0:8001 --workers 3 --timeout 120
      "
    labels:
      - traefik.enable=true
      - traefik.docker.network=n8n_traefik-public
      - traefik.http.services.django-v2.loadbalancer.server.port=8001

      # tenants: /api/* except n8n integration prefix (that prefix has its own router below)
      - traefik.http.routers.django-v2-api.rule=(${TRAEFIK_BACKEND_V2_HOST_RULE}) && (${TRAEFIK_BACKEND_V2_API_PATH_RULE})
      - traefik.http.routers.django-v2-api.entrypoints=web,websecure
      - traefik.http.routers.django-v2-api.tls=true
      - traefik.http.routers.django-v2-api.tls.certresolver=mytlschallenge
      - traefik.http.routers.django-v2-api.service=django-v2
      - traefik.http.routers.django-v2-api.priority=1100

      # Integration API: must beat Host-only n8n webhooks on the same hosts (priority > django-v2-api)
      - traefik.http.routers.django-v2-n8n.rule=${TRAEFIK_BACKEND_V2_N8N_RULE}
      - traefik.http.routers.django-v2-n8n.entrypoints=web,websecure
      - traefik.http.routers.django-v2-n8n.tls=true
      - traefik.http.routers.django-v2-n8n.tls.certresolver=mytlschallenge
      - traefik.http.routers.django-v2-n8n.service=django-v2
      - traefik.http.routers.django-v2-n8n.priority=1200
    networks:
      traefik-public:
      backend:
        aliases:
          - kolberg-django-v2
      gateway:
        aliases:
          - kolberg-django-v2

  tg-gateway:
    build: ./tg-gateway
    container_name: tg_gateway
    restart: always
    environment:
      BACKEND_WEBHOOK_URL: http://kolberg-django-v2:8001/api/messaging-gateway/webhook/
      PUBLIC_WEBHOOK_BASE_URL: ${TG_GATEWAY_PUBLIC_BASE_URL:-https://tg-gateway.kolberg.uz}
      CALLBACK_COOLDOWN_SECS: ${CALLBACK_COOLDOWN_SECS:-10}
      DATABASE_URL: ${TG_GATEWAY_DATABASE_URL:-}
    labels:
      - traefik.enable=true
      - traefik.docker.network=n8n_traefik-public
      - traefik.http.services.tg-gateway.loadbalancer.server.port=8080
      # External access is limited strictly to Telegram webhook delivery path.
      - traefik.http.routers.tg-gateway-webhook.rule=Host(`${TG_GATEWAY_PUBLIC_HOST:-tg-gateway.kolberg.uz}`) && PathPrefix(`/v1/messaging/webhook/`)
      - traefik.http.routers.tg-gateway-webhook.entrypoints=web,websecure
      - traefik.http.routers.tg-gateway-webhook.tls=true
      - traefik.http.routers.tg-gateway-webhook.tls.certresolver=mytlschallenge
      - traefik.http.routers.tg-gateway-webhook.service=tg-gateway
      - traefik.http.routers.tg-gateway-webhook.priority=1300
    depends_on:
      - backend_v2
    networks:
      # Must share traefik-public with Traefik; otherwise the Docker provider may use the
      # backend-network IP and Traefik (traefik-public only) gets intermittent dial timeouts.
      - traefik-public
      - backend
      - gateway

  frontend_v2:
    build: ./frontend_v2
    container_name: frontend_v2
    restart: always
    labels:
      - traefik.enable=true
      - traefik.docker.network=n8n_traefik-public
      - traefik.http.services.frontend-v2.loadbalancer.server.port=80

      # tenants: /app/*
      - traefik.http.routers.frontend-v2.rule=(${TRAEFIK_FRONTEND_HOST_RULE}) && (${TRAEFIK_FRONTEND_APP_PATH_RULE})
      - traefik.http.routers.frontend-v2.entrypoints=web,websecure
      - traefik.http.routers.frontend-v2.tls=true
      - traefik.http.routers.frontend-v2.tls.certresolver=mytlschallenge
      - traefik.http.routers.frontend-v2.service=frontend-v2
      - traefik.http.routers.frontend-v2.priority=1050

    networks:
      - traefik-public

  staticfiles:
    image: nginx:alpine
    container_name: staticfiles
    restart: always
    volumes:
      - ./staticfiles/conf.d:/etc/nginx/conf.d:ro
      - ./backend/staticfiles:/usr/share/nginx/static:ro
    labels:
      - traefik.enable=true
      - traefik.docker.network=n8n_traefik-public
      - traefik.http.services.staticfiles.loadbalancer.server.port=80

      # ---- login.kolberg.uz /static/* ----
      - traefik.http.routers.staticfiles-login.rule=Host(`login.kolberg.uz`) && PathPrefix(`/static/`)
      - traefik.http.routers.staticfiles-login.entrypoints=web,websecure
      - traefik.http.routers.staticfiles-login.tls=true
      - traefik.http.routers.staticfiles-login.tls.certresolver=mytlschallenge
      - traefik.http.routers.staticfiles-login.service=staticfiles
      - traefik.http.routers.staticfiles-login.priority=2000

      # ---- tenants /static/* (добавь сюда нужные хосты) ----
      - traefik.http.routers.staticfiles-tenants.rule=(Host(`lemonfit.kolberg.uz`) || Host(`lemonfit2.kolberg.uz`) || Host(`neuron.kolberg.uz`) || Host(`lemonaqua.kolberg.uz`)) && PathPrefix(`/static/`)
      - traefik.http.routers.staticfiles-tenants.entrypoints=web,websecure
      - traefik.http.routers.staticfiles-tenants.tls=true
      - traefik.http.routers.staticfiles-tenants.tls.certresolver=mytlschallenge
      - traefik.http.routers.staticfiles-tenants.service=staticfiles
      - traefik.http.routers.staticfiles-tenants.priority=2000

    networks:
      - traefik-public

  wg-easy:
    image: ghcr.io/wg-easy/wg-easy:latest
    container_name: wg-easy
    network_mode: "host"
    environment:
      WG_HOST: 85.9.200.143
      WG_DEFAULT_ADDRESS: 10.8.0.x
      WG_DEFAULT_DNS: 8.8.8.8
      # IPv6 via VPN often breaks on VPS without v6 routing; ::/0 + bad path → “half-loaded” pages. Use 0.0.0.0/0 only if you need v6.
      WG_ALLOWED_IPS: 0.0.0.0/0
      # Outbound NIC for NAT in wg0.conf PostUp (`ip route get 8.8.8.8` → dev …). Wrong default breaks internet after reboot.
      WG_DEVICE: eth2
      PASSWORD_HASH: ${WG_PASSWORD_HASH}
    volumes:
      - ./wg:/etc/wireguard
    cap_add:
      - NET_ADMIN
      - SYS_MODULE
    restart: unless-stopped

  mcp-postgres:
    image: node:22-alpine
    container_name: mcp-postgres
    restart: always
    command: >
      sh -c "
        npm install -g @modelcontextprotocol/server-postgres mcp-proxy &&
        mcp-proxy --port 3100 --transport sse npx @modelcontextprotocol/server-postgres 'postgresql://${POSGRES_MCP_USER}:${POSGRES_MCP_PASSWORD}@db:5432/backend_v2'
      "
    depends_on:
      db:
        condition: service_healthy
    labels:
      - traefik.enable=true
      - traefik.docker.network=n8n_traefik-public
      - traefik.http.routers.mcp-postgres.rule=Host(`mcp-db.kolberg.uz`)
      - traefik.http.routers.mcp-postgres.entrypoints=web,websecure
      - traefik.http.routers.mcp-postgres.tls=true
      - traefik.http.routers.mcp-postgres.tls.certresolver=mytlschallenge
      - traefik.http.services.mcp-postgres.loadbalancer.server.port=3100
    networks:
      - traefik-public
      - backend

volumes:
  n8n_data:
  traefik_data:
  pg_data:
  portainer_data:
  claude_sessions:
  garlic-hub-public-var:
  garlic-hub-var:
# >>> CHANGE: добавили сети
networks:
  traefik-public:
    name: proxy
  backend:
  chromium:
  gateway:
    internal: true
