server {
  listen 80;
  server_name _;

  # Canonicalize /app without trailing slash.
  location = /app {
    return 301 /app/;
  }

  # SPA is served under /app/
  location /app/ {
    root /usr/share/nginx/html;
    try_files $uri $uri/ /app/index.html;
  }
}

